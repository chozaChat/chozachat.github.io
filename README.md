
  # Chatting app with accounts

  This is a code bundle for Chatting app with accounts. The original project is available at https://www.figma.com/design/99SfoUekbtcRoXl8svg5LS/Chatting-app-with-accounts.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  